import { OrderStatusPercentagePipe } from './order-status-percentage.pipe';

describe('OrderStatusPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderStatusPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
